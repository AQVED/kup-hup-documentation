# Основні можливості VS Code на прикладі проекту KupHup (Telegram бот маркетплейсу)

## 1. IntelliSense (Інтелектуальне завершення коду)

### Як це працює у проекті:
- **Автодоповнення імпортів**: При вводі `from aiogram import` VS Code пропонує доступні модулі
- **Підказки типів**: До параметрів функцій (наприклад, `message: types.Message`, `state: FSMContext`)
- **Контекстні підказки**: При роботі зі JSON файлами користувацьких даних

### Приклади в коді:
```python
# Коли вводимо "message.", VS Code показує всі методи Message
@dp.message(Command("start"))
async def cmd_start(message: types.Message, state: FSMContext):
    await message.answer(...)  # IntelliSense показує методи Message
```

---

## 2. Відладка (Debugging)

### Налаштування для проекту:
- Можна встановити точки зупину (breakpoints) перед важливими функціями
- Перегляд локальних змінних у кожній точці зупину
- Крок за кроком виконання коду

### Точки для відладки в KupHup:
- Функції завантаження/збереження JSON файлів
- FSM обробники станів
- Обробка повідомлень від користувачів

```python
# Тут можна встановити breakpoint для перевірки якого товару видаляємо
def load_user_products(user_id: int) -> list:
    file = get_user_file(user_id)
    # breakpoint()  <- точка для відладки
    if file.exists():
        ...
```

---

## 3. Git інтеграція

### Можливості в VS Code:
- Перегляд історії змін (Source Control)
- Порівняння версій файлів (Diff)
- Коміти та управління гілками
- Виявлення конфліктів злиття

### Застосування до KupHup:
```
Стандартна структура Git репозиторієм:
- main.py          (основний бот)
- user_data/       (JSON з даними користувачів - .gitignore)
- .env             (токен - .gitignore)
- requirements.txt (залежності)
```

---

## 4. Пошук і заміна (Find & Replace)

### Використання в проекті:
- **Ctrl+F**: Пошук функції (наприклад, `load_ratings` використовується в 5 місцях)
- **Ctrl+H**: Заміна однакових назв (наприклад, перейменування `seller_id` → `vendor_id`)
- **Ctrl+Shift+F**: Пошук у всіх файлах проекту

### Приклад:
```
Пошук: "category_buttons"
Результати: 8 входжень
- Два рази у визначенні
- Чотири рази в функціях клавіатури
- Два рази у фільтрації
```

---

## 5. Рефакторинг (F2 - перейменування)

### Можливості VS Code:
- **Безпека**: Перейменування впливає на все місце вживання символу
- **Автоматизація**: Замість ручної заміни в кожному місці

### Приклади для KupHup:
```python
# Користувач хоче перейменувати функцію
load_user_products() → get_seller_products()

# VS Code перейменує її в:
# - Визначенні функції
# - Усіх вызовах (у SellerMenu, добавленню товару, тощо)
# - JSON завантаженнях
```

---

## 6. Code Snippets (Фрагменти коду)

### Вбудовані фрагменти:
- `try` → Генерує try/except блок
- `for` → Цикл for
- `asyncdef` → Асинхрона функція

### Користувацькі сніпети для проекту:
```python
# Фрагмент для обробника команди
{
    "Telegram Handler": {
        "prefix": "tg_handler",
        "body": [
            "@dp.message(${1:Filter})",
            "async def ${2:handler_name}(message: types.Message, state: FSMContext):",
            "    ${3:# код}"
        ]
    }
}
```

---

## 7. Вбудований терміналь

### Використання у проекті:
- **python main.py** - запуск бота
- **pip install -r requirements.txt** - встановлення залежностей
- **git status** - перевірка статусу репозиторію
- **pip freeze > requirements.txt** - оновлення списку залежностей

### Команди для KupHup:
```powershell
# Встановлення залежностей
pip install aiogram python-dotenv

# Запуск бота
python main.py

# Перевірка синтаксису
python -m py_compile main.py
```

---

## 8. Розширення (Extensions)

### Рекомендовані для цього проекту:

| Розширення | Призначення |
|-----------|-----------|
| **Python** | Автодоповнення, відладка, linting |
| **Pylint** | Аналіз якості коду |
| **Better Comments** | Кольорові коментарі |
| **JSON** | Форматування JSON файлів |
| **Thunder Client** | Тестування API (для REST інтеграції) |
| **GitLens** | Розширена інформація Git |

---

## 9. Структура проекту в Explorer

### Навігація у файлах:
```
KupHup/
├── main.py              (основний файл)
├── requirements.txt     (залежності)
├── .env                (телеграм токен)
├── .gitignore          (ігнорування файлів)
├── user_data/
│   ├── 12345.json      (товари користувача)
│   ├── ratings.json    (оцінки продавців)
│   └── favorites.json  (обране користувачів)
└── docs/               (документація)
```

### Швидка навігація:
- **Ctrl+P**: Відкрити файл по назві
- **Ctrl+Shift+.**: До визначення функції
- **Alt+↑/↓**: Переміщення рядків кода

---

## 10. Форматування коду (Prettier, Black)

### Для Python проекту:
```python
# До форматування (неакуратно):
def get_seller_rating(seller_id:int)->tuple:
    ratings=load_ratings()
    seller_data=ratings.get(str(seller_id),{})
    rating_list=seller_data.get('ratings',[])

# Після форматування (Black):
def get_seller_rating(seller_id: int) -> tuple:
    ratings = load_ratings()
    seller_data = ratings.get(str(seller_id), {})
    rating_list = seller_data.get("ratings", [])
```

### Налаштування у .vscode/settings.json:
```json
{
    "python.formatting.provider": "black",
    "[python]": {
        "editor.formatOnSave": true,
        "editor.defaultFormatter": "ms-python.python"
    }
}
```

---

## 11. Проблеми та лінтинг (Problems Panel)

### Виявлення помилок у KupHup:

| Помилка | Де виявляється |
|--------|-------|
| Невикористані імпорти | На лінії `import json` (якщо не використовується) |
| Невізначені змінні | На `category_buttons` перед визначенням FSM |
| Type hints мисматчи | `def build_product_keyboard(products: list)` (краще `List[Dict]`) |
| Bare except | `except:` без специфічного типу помилки |

### Виправлення:
```python
# ❌ Проблема
except:
    return []

# ✅ Рішення
except (FileNotFoundError, json.JSONDecodeError):
    return []
```

---

## 12. Керування залежностями через VS Code

### Файл requirements.txt:
```
aiogram==3.0.0
python-dotenv==1.0.0
```

### Встановлення через VS Code терміналь:
```bash
pip install -r requirements.txt
```

---

## 13. Налаштування робочої області (.vscode/settings.json)

```json
{
    "python.linting.enabled": true,
    "python.linting.pylintEnabled": true,
    "python.linting.pylintArgs": ["--max-line-length=100"],
    "editor.rulers": [80, 100],
    "editor.indentSize": 4,
    "editor.insertSpaces": true,
    "files.exclude": {
        "**/__pycache__": true,
        "**/*.pyc": true,
        "user_data/": false
    }
}
```

---

## Швидкі клавіші для продуктивності

| Клавіша | Дія |
|--------|-----|
| **Ctrl+`** | Відкрити терміналь |
| **Ctrl+B** | Сховати Explorer |
| **Ctrl+J** | Сховати/показати панель |
| **F12** | До визначення (Go to Definition) |
| **Shift+F12** | Все вживання (Find All References) |
| **Ctrl+.** | Швидке виправлення (Quick Fix) |
| **Ctrl+Shift+P** | Command Palette (всі команди) |

---

## 14. Командна робота (Collaboration)

### 14.1 Live Share - Спільне редагування в реальному часі

**Можливості:**
- Кілька розробників редагують один файл одночасно
- Спільна сесія відладки (Shared Debugging)
- Спільна консоль (Shared Terminal)
- Спільний сервер для тестування

**Приклад для KupHup:**
```
Сценарій: Два розробники працюють над ботом
- Розробник 1: Пише обробник для режиму продавця
- Розробник 2: Пише обробник для режиму покупця
- Вони бачать друг друга діяльність в реальному часі
- Можуть разом відлагоджувати FSM стани
```

**Як запустити Live Share:**
1. **Ctrl+Shift+P** → Команда "Live Share: Start collaboration session"
2. Поділитися посиланням зі спільпрацівником
3. Спільпрацівник приєднується до сесії
4. Спільне редагування починається негайно

---

### 14.2 GitHub Pull Requests & Issues

**Інтеграція з GitHub:**
- Перегляд і написання PR прямо у VS Code
- Обговорення змін в контексті коду
- Перевірка кількості лайків на коміти
- Управління Issues проекту

**Робочий процес для KupHup:**
```
1. Розробник створює гілку: git checkout -b feature/seller-ratings
2. Пише код (наприклад,새 рейтинг система)
3. Комітить зміни локально
4. Пушить гілку

5. У VS Code → GitHub Pull Requests розширення:
   - Можна переглянути diff коду
   - Написати опис PR
   - Запросити review
   - Обговорювати зміни в лінію коду

6. Після approve — merge у main
```

**Приклад PR опису:**
```markdown
## Додав систему оцінки продавців ⭐

### Що змінено:
- Додана FSM стан для оцінки (RateSellerState)
- Функції для завантаження/збереження рейтингів
- Клавіатура з 5 зірочками

### Тестовано:
- ✅ Оцінка зберігається в ratings.json
- ✅ Рейтинг розраховується правильно
- ✅ Коментарі додаються до продавця

### Closes: #42
```

---

### 14.3 Code Review & Comments

**Коментування коду під час PR:**

```python
// Comment from reviewer in VS Code:
// ❌ Це займає багато пам'яті, можна оптимізувати
def load_all_products() -> dict:
    """Завантажує всі товари всіх продавців"""
    all_products = {}
    for file in DATA_DIR.glob("*.json"):  // 👈 Пропозиція
        # Краще використати список розуміння або кеш
```

**Типи коментарів:**
- **Питання**: "Чому ми не використовуємо кеш тут?"
- **Пропозиція**: "Перейменуй `seller_id` на `vendor_id` для консистентності"
- **Похвала**: "Гарна оптимізація! 👍"

---

### 14.4 Shared Terminal для командної роботи

**Сценарій:**
```powershell
# Двоє розробників у Live Share сесії

# Розробник 1 запускає тести:
PS> pytest tests/test_bot.py

# Розробник 2 бачить результати в реальному часі
# Якщо тест падає, вони разом видаляють умови
# Один вводить команду, другий бачить результат
```

**Команди для спільної роботи:**
```bash
# Встановлення залежностей разом
pip install -r requirements.txt

# Запуск бота для тестування
python main.py

# Перевірка синтаксису перед commititом
python -m py_compile main.py

# Git операції спільно
git status
git log --oneline
```

---

### 14.5 Синхронізація VS Code налаштувань

**Settings Sync - одні налаштування для всієї команди:**

Налаштування, що синхронізуються:
- Теми оформлення
- Розширення
- Скорочення клавіш (keybindings)
- Користувацькі налаштування
- Code Snippets

**Для KupHup:**
```json
{
    "editor.defaultFormatter": "ms-python.python",
    "python.linting.enabled": true,
    "[python]": {
        "editor.formatOnSave": true,
        "editor.codeActionsOnSave": {
            "source.organizeImports": "explicit"
        }
    },
    "workbench.colorTheme": "One Dark Pro"
}
```

Усі члени команди матимуть:
- ✅ Одне форматування (Black, 4 пробіла)
- ✅ Одні й ті ж расширення
- ✅ Одні й ті ж скорочення клавіш

---

### 14.6 GitHub Copilot для командної роботи

**AI асистент на допомогу команді:**

**Приклад 1: Автодоповнення для функції обробника:**
```python
@dp.message(F.text == "💼 Режим продавця")
async def seller_mode(message: types.Message, state: FSMContext):
    # Copilot пропонує завершення:
    # - Завантажити товари користувача
    # - Отримати рейтинг продавця
    # - Відправити відповідь з меню
    
    await state.clear()
    products = load_user_products(message.from_user.id)  # <- Copilot це вже пропонував
```

**Приклад 2: Генерація документації:**
```python
def save_user_products(user_id: int, products: list):
    # Copilot генерує docstring:
    """
    Зберігає список товарів користувача у JSON файл.
    
    Args:
        user_id: ID користувача (продавця)
        products: Список товарів для збереження
        
    Returns:
        None (зберігає безпосередньо у файл)
    """
```

---

### 14.7 Управління проектом в VS Code

**Extensions для командної роботи:**

| Розширення | Призначення | Для команди |
|-----------|-----------|-----------|
| **GitHub Pull Requests** | Управління PR в VS Code | Код-ревʻю |
| **GitLens** | Історія комітів і авторства | Відстеження змін |
| **Live Share** | Спільне редагування | Спільна сесія |
| **Comment Anchors** | Закладки в коді (`@anchor`) | Навігація по завданням |
| **Todo Tree** | Управління TODO завданнями | Спільний чек-лист |
| **Peacock** | Різні кольори для різних папок | Розрізнення багатопроектності |

**Встановлення для команди:**
```bash
# Командне розширення можна експортувати
code --install-extension eamodio.gitlens
code --install-extension ms-vsliveshare.vsliveshare
code --install-extension GitHub.vscode-pull-request-github
```

---

### 14.8 Branching Strategy для KupHup

**Git Flow для командної роботи:**

```
main (production)
 ↑
 └─ release/v1.0
     ↑
     └─ develop (development)
         ├─ feature/seller-ratings (Developer 1)
         ├─ feature/buyer-search (Developer 2)
         ├─ feature/favorites (Developer 3)
         └─ bugfix/json-encoding (Developer 4)
```

**Команди у VS Code:**

1. **Переключення гілок:**
   ```bash
   git checkout -b feature/seller-ratings
   ```

2. **Push у remote:**
   ```bash
   git push -u origin feature/seller-ratings
   ```

3. **Pull Request через VS Code:**
   - Розширення "GitHub Pull Requests"
   - Клік "Create Pull Request"
   - Вибір базової гілки (develop)
   - Описати зміни
   - Запросити reviewer

4. **Merge після approval:**
   ```bash
   git merge feature/seller-ratings
   ```

---

### 14.9 Спільні документи та комунікація

**VS Code як центр спільної роботи:**

- **Markdown документи**: README проекту, архітектура
- **Live Share Chat**: Обговорення під час спільного редагування
- **Comments на коді**: Обговорення специфічних рядків
- **Issues & Discussions**: Планування функцій

**Структура документів для KupHup:**

```
docs/
├── README.md              (опис проекту)
├── ARCHITECTURE.md        (архітектура бота)
├── API_REFERENCE.md       (aiogram API посилання)
├── CONTRIBUTION.md        (як контрибютити)
├── SETUP.md              (установка для розробників)
└── CHANGELOG.md          (історія змін)
```

---

### 14.10 CI/CD інтеграція в VS Code

**Статус pipeline у VS Code:**

```python
# Після push у GitHub
# VS Code показує статус:

✅ Tests  [passed]  (4/4 tests)
✅ Linting [passed]  (no errors)
⏳ Deploy [pending]
```

**GitHub Actions для KupHup:**
```yaml
name: Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run tests
        run: pytest tests/
```

---

## Висновок

VS Code забезпечує **комплексне середовище розробки** для проекту KupHup, включаючи:
- ✅ Код автодоповнення для aiogram бота
- ✅ Управління JSON даними користувачів
- ✅ Вбудована відладка для FSM станів
- ✅ Git інтеграція для версіонування
- ✅ Розширення для підвищення продуктивності
- ✅ **Командна робота**: Live Share, PR review, спільні сесії
- ✅ **Управління проектом**: Issues, спільні документи, CI/CD

Для команди розробників KupHup це створює:
- 🚀 Швидкий цикл розробки
- 🤝 Ефективну комунікацію
- ✨ Якісний код через review
- 📊 Повне відстеження змін

Це робить розробку, тестування та розташування коду значно легшим і швидшим при командній роботі! 🚀
